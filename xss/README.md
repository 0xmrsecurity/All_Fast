# Usage

This tools get the victim cookies. Xss Attack when happen if unfortunately someone click the payload you send . Important think is that if the website have `HTTPONLY` is false, you are do this attack.

## Fast_Xss.sh
```bash
bash Fast_Xss.sh <IP> <Port>
```
## Fast_Xss.py
```bash
python3 Fast_Xss.py --lhost <IP> --lport <Port>
```
